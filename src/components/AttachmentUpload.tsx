'use client';
import {useState} from 'react';
export default function AttachmentUpload({taskId,projectId,onSaved}:{taskId:string;projectId:string;onSaved:()=>Promise<void>}){
 const [busy,setBusy]=useState(false),[progress,setProgress]=useState(0),[error,setError]=useState('');
 return <form className="space-y-2" onSubmit={async e=>{e.preventDefault();if(busy)return;const form=e.currentTarget;const data=new FormData(form);data.set('taskId',taskId);data.set('projectId',projectId);setBusy(true);setProgress(0);setError('');try{await new Promise<void>((resolve,reject)=>{const xhr=new XMLHttpRequest();xhr.open('POST','/api/attachments/upload');xhr.upload.onprogress=event=>{if(event.lengthComputable)setProgress(Math.round(event.loaded/event.total*100));};xhr.onerror=()=>reject(new Error('Upload interrupted. Check the attachment list before retrying.'));xhr.onload=()=>{try{const r=JSON.parse(xhr.responseText);if(xhr.status>=200&&xhr.status<300&&!r.error)resolve();else reject(new Error(r.error||'Upload failed.'));}catch{reject(new Error('Upload failed.'));}};xhr.send(data);});form.reset();await onSaved();}catch(e){setError(e instanceof Error?e.message:'Upload failed.');}finally{setBusy(false);}}}><input type="file" name="file" required disabled={busy} aria-label="Task attachment" className="w-full text-sm"/><p className="text-xs text-gray-500">Up to 10 MB. Visible only to people with project access.</p>{busy&&<div role="status"><progress value={progress} max={100} aria-label="Upload progress" className="w-full"/><p className="text-xs">{progress<100?`Uploading ${progress}%`:'Saving attachment…'}</p></div>}{error&&<p role="alert" className="text-sm text-red-700">{error}</p>}<button disabled={busy} className="rounded-lg border px-3 py-2 text-sm">Upload file</button></form>;
}
