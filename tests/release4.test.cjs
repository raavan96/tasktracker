/* eslint-disable @typescript-eslint/no-require-imports */
const {test}=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const ts=require('typescript');const path=require('node:path');
function load(name){const m={exports:{}};new Function('exports',ts.transpileModule(fs.readFileSync(path.join(__dirname,'../src/lib/'+name+'.ts'),'utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022}}).outputText)(m.exports);return m.exports;}
const {scheduleDates}=load('schedule-dates'),{previewMime}=load('attachment-preview');
test('monthly preview preserves month-end anchor through February',()=>{assert.deepEqual(scheduleDates('2028-01-31','monthly','2028-01-31','2028-04-30'),['2028-01-31','2028-02-29','2028-03-31','2028-04-30']);});
test('schedule previews respect inclusive end and five occurrence limit',()=>{assert.deepEqual(scheduleDates('2026-09-14','weekly','2026-09-14','2026-09-21'),['2026-09-14','2026-09-21']);assert.equal(scheduleDates('2026-09-14','daily','2026-09-14',null).length,5);assert.deepEqual(scheduleDates('invalid','daily','invalid',null),[]);});
test('preview content sniffing rejects active web documents and recognizes allowed headers',()=>{assert.equal(previewMime(Buffer.from('<svg onload="alert(1)"></svg>')),null);assert.equal(previewMime(Buffer.from('<html>fake.png</html>')),null);assert.equal(previewMime(Buffer.from('%PDF-1.7\n')), 'application/pdf');assert.equal(previewMime(Uint8Array.from([137,80,78,71,13,10,26,10])), 'image/png');assert.equal(previewMime(Buffer.from('')),null);});
